package com.example.config;

// Intentionally left empty; default Spring Boot scanning will pick up entities and repositories
public class JpaConfig {
}
// why this file exists:
// bcos it's needed to ensure that Spring Boot scans the "com.example" package for JPA entities and repositories.